package it.polimi.ingsw.controller;

/**
 * This enumeration contains the two possible states of a game ({@code SETUP} if the game controller is still accepting
 * players, {@code IN_GAME} if the game has started).
 */

public enum GameState {
    SETUP, IN_GAME
}
